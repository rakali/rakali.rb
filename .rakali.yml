from:
  folder: examples
  format: docx
to:
  folder: output
  format: pdf
schema: title_block.json
merge: false
strict: true
options:
  latex-engine: xelatex
variables:
  documentclass: article
filters:
  - behead2.hs
